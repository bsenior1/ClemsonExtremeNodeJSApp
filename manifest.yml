applications:
- path: .
  memory: 256M
  instances: 1
  domain: mybluemix.net
  name: ${VCAP_APPLICATION.application_name}
  host: ${VCAP_APPLICATION.application_name}
  disk_quota: 1024M
  services:
  - Weather Company Data-z4
